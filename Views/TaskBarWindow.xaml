<hc:Window
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
          xmlns:hc="https://handyorg.github.io/handycontrol"
          xmlns:local="clr-namespace:SplusXBTMeter"
          x:Class="SplusXBTMeter.TaskBarWindow"
          Title="TaskBarWindow" 
          Height="46"
          Width="200"
          WindowStyle="None"
          AllowsTransparency="True"
          Background="Transparent"
          ShowInTaskbar="False"
          BorderThickness="0"
          Topmost="False"
          Icon="pack://application:,,,/app.ico"
          ResizeMode="NoResize">

    <hc:Window.Resources>
        <!-- 水波纹样式（无改动） -->
        <Style x:Key="WaveProgressBarStyle" TargetType="{x:Type hc:WaveProgressBar}" BasedOn="{StaticResource ProgressBarWarningWave}">
            <Setter Property="ShowText" Value="False"/>
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding DataContext.IsWaveEffect, RelativeSource={RelativeSource AncestorType={x:Type hc:Window}}}" Value="True">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- 圆环样式（无改动） -->
        <Style x:Key="CircleProgressBarStyle" TargetType="{x:Type hc:CircleProgressBar}" BasedOn="{StaticResource {x:Type hc:CircleProgressBar}}">
            <Setter Property="Visibility" Value="Visible"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding DataContext.IsWaveEffect, RelativeSource={RelativeSource AncestorType={x:Type hc:Window}}}" Value="True">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </hc:Window.Resources>

    <Grid Height="46" VerticalAlignment="Center">
        <ItemsControl 
            ItemsSource="{Binding BluetoothDevices}"
            Height="46"
            VerticalAlignment="Center"
            HorizontalAlignment="{Binding TaskBarAlignment}">
            <!-- 🔥 核心：安全靠右，不破坏任何样式 -->

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <!-- 完全还原你原来的布局，删除所有破坏性属性 -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,0"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <!-- 👇 你的原有设备卡片代码 100% 不动，完全保留 -->
                    <hc:SimplePanel Height="46" Width="46" Margin="0,0,0,0" >
                        <hc:SimplePanel.Style>
                            <Style TargetType="{x:Type hc:SimplePanel}">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <!-- 当 IsShow 为 false 时隐藏 -->
                                    <DataTrigger Binding="{Binding IsShow}" Value="False">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <!-- 当 IsConnected 为 false 时隐藏 -->
                                    <DataTrigger Binding="{Binding IsConnected}" Value="False">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </hc:SimplePanel.Style>

                        <Grid Width="34" Height="34" Margin="0,2,0,0" HorizontalAlignment="Center" VerticalAlignment="Top">
                            <hc:CircleProgressBar Style="{StaticResource CircleProgressBarStyle}" Background="#FFC8CACA" Width="34" Height="34" FontSize="8" ShowText="False" Value="{Binding Battery, FallbackValue=0, TargetNullValue=0}"/>
                            <hc:WaveProgressBar Style="{StaticResource WaveProgressBarStyle}" Value="{Binding Battery, FallbackValue=0}" Width="34" Height="34" WaveThickness="3">
                                <hc:WaveProgressBar.WaveFill>
                                    <SolidColorBrush Color="#309430" Opacity="0.8"/>
                                </hc:WaveProgressBar.WaveFill>
                            </hc:WaveProgressBar>
                            <TextBlock Text="{Binding Battery, StringFormat=\{0\}%}" FontSize="8" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>

                        <Label Height="10" 
                               Width="46" Background="Transparent" 
                               Foreground="{DynamicResource TextPrimaryBrush}" 
                               FontSize="8" BorderThickness="0" Padding="0" 
                               VerticalAlignment="Top" Margin="0,36,0,0" 
                               Content="{Binding Name, FallbackValue=未知设备}"/>
                    </hc:SimplePanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</hc:Window>